const router = require('express').Router


router.post('/api/login', async (req, res) => {
	//Recieve Google code -> get google token -> get userID
	//if userID exists ? send jwt token : create user and send jwt token
})

router.post('/api/login', async (req, res) => {

})